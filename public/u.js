console.log("// u.js //")

window.D = document
window.B = D.body

window.GEBI = id=>D.getElementById(id)

window.len   = x=>x.length

window.loads = x=>JSON.parse
window.dumps = x=>JSON.stringify
window.str   = x=>JSONify

window.None = null
